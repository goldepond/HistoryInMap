{
  "name": "history-world-map",
  "version": "4.0.0",
  "description": "연도별 세계사 지도 — 정적 사이트(읽기 전용). 콘텐츠는 content/*.md(위키/Terraform식)로 git 관리.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-borders.mjs && node scripts/build-manifest.mjs",
    "build:borders": "node scripts/build-borders.mjs",
    "build:manifest": "node scripts/build-manifest.mjs",
    "start": "node server.mjs",
    "fetch-borders": "node scripts/fetch-borders.mjs"
  },
  "license": "GPL-3.0"
}
